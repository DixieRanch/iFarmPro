 <% provide :title, 'Shipment Selection' %>
 
 <%= simple_form_for @shipment_selection, html: {class: 'form-inline'} do |f| %>
     <%= f.input :shipment_id,
                 collection: Shipment.all.sort_by(&:name),
                 label_method: :name,
                 include_blank: false 
     %>
 <% end %>