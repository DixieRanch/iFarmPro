<% provide :title,  @freezer_location.name%>
    <table id='lots' class='table table-striped'>
        <thead>
            <th> Lot Name</th>
            <th> Box Name</th>
            <th> Contnents</th>
        </thead>
        
        <tbody>
            <% @lots.each_with_index do |lot, l| %>
            <tr>
                <td id="name<%=l%>"><%= lot.name %></td>
                <td id="name<%=l%>"><%= lot.box.name %></td>
                <td id="name<%=l%>"><%= lot.content.name %></td>
            </tr>
             <% end %>
        </tbody>
    </table>