<% provide :title, 'Storage Management' %>
<table id='locations' class='table table-striped'>
  <thead>
    <h1>Location Overview</h1>
    <tr>
      <th>Location Name</th>
      <th>Net Weight</th>
      <th>Lot Count</th>
    </tr>
  </thead>
  <tbody>
    <h3>Total Net Weight: <%= @total_net_weight %></h3>
    <h3>Total Lot Count: <%= @total_lot_count %></h3>
    <% @loads.each_with_index do |ld, l| %>
    <tr>
      <td id="name<%=l%>"><%= ld.location.name %></td>
      <td id="net_weight<%=l%>"><%= ld.weight %></td>
      <td id="lot_count<%=l%>"><%= ld.lot_total %></td>
      <td>
        <%= link_to 'Move All Lots', edit_load_path(ld.location), 
                                     id:"link_#{l}",
                                     class: 'btn-sm btn-primary',
                                     role: 'button'
        %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
<%= will_paginate @loads %>